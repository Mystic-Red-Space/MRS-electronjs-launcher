language: node_js
node_js:
  - 10
env: ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES=true CC=clang CXX=clang++ npm_config_clang=1 YARN_GPG=no
osx_image: xcode8.3 
jobs:
  fast_finish: true
  include:
    - os: linux
      before_install: sudo apt-get install --no-install-recommends -y icnsutils graphicsmagick xz-utils
      compiler: clang
    - os: windows
      compiler: clang
    - os: osx
      before_install: brew install git-lfs
      script: travis-wait yarn run build 
cache:
  - npm
  - yarn
  - directories:
    - node_modules
    - "$HOME/.electron"
    - "$HOME/.cache"
install:
  - npm install -g xvfb-maybe
  - yarn install
script:
  - yarn run build 
branches:
  only:
    - vue
